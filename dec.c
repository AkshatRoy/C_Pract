#include <stdio.h>

void main()
{

    //     int a, b;
    //     printf("Enter the X Co-ordinates : ");
    //     scanf("%d", &a);
    //     printf("Enter the Y Co-ordinates : ");
    //     scanf("%d", &b);

    //     if (a > 0)
    //     {
    //         if (b > 0)
    //         {
    //             printf("Co-ordinates are in 1st Quad");
    //         }
    //         else
    //         {
    //             printf("Co-ordinates are in 2nd Quad");
    //         }
    //     }

    //     else
    //     {
    //         if (b > 0)
    //         {
    //             printf("Co-ordinates are in 4st Quad");
    //         }
    //         else
    //         {
    //             printf("Co-ordinates are in 3nd Quad");
    //         }
    //     }

    // int a;
    // printf("Enter the Year :");
    // scanf("%d", &a);

    // if (a % 400 == 0)
    //     printf("%d is a Leap year", a);

    // else if (a % 100 == 0)
    //     printf("%d is Not a Leap Year", a);

    // else if (a % 4 == 0)
    //     printf("%d is a Leap Year", a);

    // else
    //     printf("Not a Leap Year");


    //=================================================================

    // int a;
    // printf("Enter a Number: ");
    // scanf("%d", &a);

    // if (a % 2 == 0)
    //     printf("%d Divisible by 2, 4, 12 \n", a);

    // else if (a % 3 == 0)
    //     printf("%d Divisible by 3", a);

    // else
    //     printf("%d is not divisible by 2, 3, 4, 12", a);

    //================================================================
    
    // int a;
    // printf("Enter the weight in kgs:");
    // scanf("%d", &a);

    // if (a <= 10)
    //     printf("No extra charges");

    // else if (a > 10 && a <= 40)
    // printf("Extra charge of 120 RS is added");

    // else if (a>40){
    // printf("Extra charge of 120 RS on the first 40 kgs is added \n"); 
    // printf("Rs. 10 is added for any weight more than 40 kgs \n");
    // printf("Added amount = %d Rs \n", (a-40)*10 ); 
    // printf("Total : %d \n", (120 +((a-40)*10))); }

    //=====================================================================
        
    


}